/* ============================================================
   MOONLIT FOUNDATION — config.js
   Single place to point the static site at the admin API.
   Change this if you deploy the API somewhere other than
   http://localhost:4000 (see server/README.md).
   ============================================================ */
window.MF_API_BASE = window.MF_API_BASE || "http://localhost:4000/api";
