# Used when the site runs on localhost / 127.0.0.1 (npm run dev).
VITE_API_BASE_LOCAL="http://localhost:4000/api"

# Used on any other hostname (the deployed site on Render).
VITE_API_BASE_PROD="https://moonlit-website-api.onrender.com/api"
